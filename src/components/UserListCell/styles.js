import { StyleSheet } from "react-native";
import { Colors, Metrics, Fonts, AppStyles } from "../../theme";

export default StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    marginVertical: Metrics.heightRatio(19),
    alignItems: 'center'
  },
})