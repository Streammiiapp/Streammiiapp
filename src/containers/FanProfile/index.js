import React, { useCallback, useRef } from 'react'
import { View, Text } from 'react-native'

const index = (props) => {

  const { } = props

  return (
    <View>
      <Text>FAN PROFILE</Text>
    </View>
  )
}

export default index
