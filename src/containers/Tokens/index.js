import React, { useCallback, useRef } from 'react'
import { View, Text } from 'react-native'

const index = (props) => {

  const { } = props

  return (
    <View>
      <Text>TOKENS</Text>
    </View>
  )
}

export default index
