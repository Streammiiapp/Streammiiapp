import React, { useCallback, useRef } from 'react'
import {
  Text as NativeText
} from 'react-native'

const index = (props) => {

  const { } = props

  return (
    <NativeText
      {...props}
    />
  )
}

export default index
